apiVersion: v1
kind: Service
metadata:
  name: interbank-service
  namespace: banka-1
spec:
  selector:
    app: interbank-service
  ports:
    - name: http
      port: 8091
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: interbank-service
  namespace: banka-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: interbank-service
  template:
    metadata:
      labels:
        app: interbank-service
    spec:
      containers:
        - name: interbank-service
          image: banka/interbank-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8091
          envFrom:
            - configMapRef:
                name: banka-app-config
            - secretRef:
                name: banka-app-secrets
          env:
            - name: SERVER_PORT
              valueFrom:
                configMapKeyRef:
                  name: banka-app-config
                  key: INTERBANK_SERVER_PORT
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: banka-app-config
                  key: INTERBANK_SPRING_PROFILES
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: db-pguser-interbank-service
                  key: pgbouncer-jdbc-uri
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-pguser-interbank-service
                  key: user
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-pguser-interbank-service
                  key: password
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 35
            periodSeconds: 15
          resources:
            requests:
              memory: 256Mi
            limits:
              memory: 512Mi
