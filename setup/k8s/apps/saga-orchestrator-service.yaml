apiVersion: v1
kind: Service
metadata:
  name: saga-orchestrator-service
  namespace: banka-1
spec:
  selector:
    app: saga-orchestrator-service
  ports:
    - name: http
      port: 8095
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: saga-orchestrator-service
  namespace: banka-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: saga-orchestrator-service
  template:
    metadata:
      labels:
        app: saga-orchestrator-service
    spec:
      containers:
        - name: saga-orchestrator-service
          image: banka/saga-orchestrator-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8095
          envFrom:
            - configMapRef:
                name: banka-app-config
            - secretRef:
                name: banka-app-secrets
          env:
            - name: SAGA_SERVICE_HOST
              value: 0.0.0.0
            - name: SAGA_SERVICE_PORT
              valueFrom:
                configMapKeyRef:
                  name: banka-app-config
                  key: SAGA_SERVER_PORT
            - name: SERVER_PORT
              valueFrom:
                configMapKeyRef:
                  name: banka-app-config
                  key: SAGA_SERVER_PORT
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: banka-app-config
                  key: SAGA_POSTGRES_DB
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: db-pguser-saga-orchestrator-service
                  key: pgbouncer-jdbc-uri
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-pguser-saga-orchestrator-service
                  key: user
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-pguser-saga-orchestrator-service
                  key: password
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 35
            periodSeconds: 15
          resources:
            requests:
              memory: 256Mi
            limits:
              memory: 512Mi
